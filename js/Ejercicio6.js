function concatenarArreglos(arr1, arr2) {
  return arr1.concat(arr2);
}

// Ejemplo de uso
const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];
console.log(concatenarArreglos(arr1, arr2)); // Salida: [1, 2, 3, 4, 5, 6]
